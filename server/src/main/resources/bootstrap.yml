# 切换生产环境地址使用
prod:
  host: 192.168.8.116
server:
  port: 8756
spring:
  application:
    name: order
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev
      label: master
#    stream:
#      bindings:
#        streamInput:
#          destination: streamExchange
#        streamOutput:
#          destination: streamExchange
#          group: streamGroup
